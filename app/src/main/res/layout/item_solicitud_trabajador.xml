<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Título de la Solicitud (Basado en la Propuesta) -->
        <TextView
            android:id="@+id/tvTituloSolicitudItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginBottom="4dp"
            tools:text="Solicitud para Reparación de Tuberías"/>

        <!-- Estado de la Solicitud -->
        <TextView
            android:id="@+id/tvEstadoSolicitudItem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:textSize="14sp"
            android:textStyle="italic"
            android:textColor="@android:color/holo_blue_dark"
            android:layout_marginBottom="8dp"
            tools:text="Estado: ENVIADA"/>

        <!-- Mensaje del Cliente -->

        <!-- Información del Cliente -->
        <TextView
            android:id="@+id/tvInfoClienteItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginBottom="4dp"
            tools:text="Cliente: Juan Pérez"/>

        <TextView
            android:id="@+id/tvContactoClienteItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="@android:color/black"
            android:layout_marginBottom="4dp"
            tools:text="Contacto: 987654321 - juan.perez@example.com"/>

        <!-- Distrito del Cliente (NUEVO) -->
        <TextView
            android:id="@+id/tvDistritoClienteItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="@android:color/black"
            android:layout_marginBottom="8dp"
            tools:text="Distrito: Santiago de Surco"/>

        <!-- Información de la Propuesta (detalles que el cliente está solicitando) -->
        <TextView
            android:id="@+id/tvDetallePropuestaItem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="@android:color/black"
            android:layout_marginBottom="8dp"
            tools:text="Propuesta: Fontanería - Reparación General (S/ 150.00)"/>

        <!-- Contenedor para los botones Aceptar y Rechazar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"> <!-- Alinea los botones a la derecha -->

            <!-- Botón Rechazar Solicitud -->
            <Button
                android:id="@+id/btnRechazarSolicitud"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Rechazar"
                android:backgroundTint="@android:color/holo_red_dark"
                android:textColor="@android:color/white"
                android:layout_marginEnd="8dp"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:textSize="16sp"/>

            <!-- Botón Aceptar Solicitud (cambiará a Finalizar) -->
            <Button
                android:id="@+id/btnAceptarSolicitud"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Aceptar"
                android:backgroundTint="@color/purple_500"
                android:textColor="@android:color/white"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:textSize="16sp"/>

        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>